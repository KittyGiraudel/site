{% assign more_link_label = link_label %}
{% assign more_link_url = link_url %}
{% assign id = title | slugify %}

<section class="Showcase" aria-labelledby="{{ id }}">
  {% if title %}<h2 class="Showcase__title {% if hidden_title %}visually-hidden{% endif %}" id="{{ id }}">{{ title }}</h2>{% endif %}
  <p class="Showcase__copy">{{ copy | markdown }}</p>

  <div class="Showcase__container">
    {% for item in items %}
    <div class="Showcase__item">
      {% include "{{ type }}_tile.liquid", item: item %}
    </div>
    {% endfor %}
  </div>

  {% if more_link_label and more_link_url %}
    <a href="{{ more_link_url }}" class="Showcase__link">{{ more_link_label }}</a>
  {% endif %}
</section>
